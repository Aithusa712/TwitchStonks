fastapi==0.111.0
uvicorn[standard]==0.30.1
# Pin motor and pymongo to compatible versions; newer pymongo releases removed
# internal symbols referenced by motor, causing import failures at startup.
motor==3.6.0
pymongo==4.7.2
pydantic-settings==2.3.4
